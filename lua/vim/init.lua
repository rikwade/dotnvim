local Option = require('vim.option')
local Variable = require('vim.variable')
local Keybind = require('vim.keybind')

local Vim = {
    Option = Option,
    Variable = Variable,
    Keybind = Keybind
}

return Vim